ccxt>=4.4.0
pandas>=2.2.0
numpy>=1.26.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
web3>=6.15.0
requests>=2.31.0
# urllib3 v2 requires OpenSSL 1.1.1+, but this machine's Python 3.9 links
# LibreSSL 2.8.3 (macOS CommandLineTools). That mismatch surfaced as
# intermittent "Hyperliquid unavailable at startup (SSLError)" in
# logs/mt5-autotrader.log, which silently dropped that venue out of the
# combined ledger and left the capital guard running on MT5 balance alone.
# Pinned below 2 so HTTPS goes through urllib3 1.x, which supports LibreSSL.
# (pip flags a conflict with types-requests, which wants urllib3>=2 — that
# package is type stubs only and is never imported at runtime.)
urllib3<2

# MT5 venue (optional — only needed for venue: mt5).
# mt5_backend: bridge — client side only; MetaTrader5 itself runs on a remote
# Windows/Wine box you provision, not here.
mt5linux>=0.1.9
# mt5_backend: metaapi — MetaApi.cloud hosts the terminal, no box needed.
metaapi-cloud-sdk>=29.1.1

# Hyperliquid DeFi wallet + perps (hyperwallet.py). eth-account comes with it.
hyperliquid-python-sdk>=0.24.0

# Local control panel (webapp/server.py) — binds 127.0.0.1 only
flask>=3.1.0

# macOS menu bar (optional — auto-installed by install-macos.sh)
# rumps>=0.4.0
# pyobjc-framework-Cocoa>=10.0
